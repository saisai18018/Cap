pipeline {
    agent any

    environment {
        GIT_REPO = 'https://github.com/saisai18018/Cap.git'
        BRANCH = 'main'
        PROJECT_DIR = 'CapDemo'
    }

    stages {
        stage('Checkout') {
            steps {
                echo "📥 Checking out code from GitHub..."
                git branch: "${BRANCH}", url: "${GIT_REPO}"
            }
        }

        stage('Build') {
            steps {
                dir("${PROJECT_DIR}") {
                    echo "⚙️ Running Maven clean and build (with tests)..."
                    bat 'mvn clean install'
                }
            }
        }

        stage('Test') {
            steps {
                dir("${PROJECT_DIR}") {
                    echo "🧪 Running TestNG suite..."
                    bat 'mvn test -Dsurefire.suiteXmlFiles=testng.xml'
                }
            }
        }

        stage('Archive Reports') {
            steps {
                dir("${PROJECT_DIR}") {
                    echo "📂 Archiving test reports..."
                    junit 'target/surefire-reports/*.xml'
                    archiveArtifacts artifacts: 'target/surefire-reports/*', fingerprint: true
                }
            }
        }
    }

    post {
        success {
            echo "✅ Build SUCCESSFUL!"
            archiveArtifacts artifacts: 'CapDemo/reports/**', fingerprint: true
        }
        failure {
            echo "❌ Build FAILED! Please check logs and reports."
            archiveArtifacts artifacts: 'CapDemo/reports/**', fingerprint: true
        }
        always {
            echo "Build finished with status: ${currentBuild.currentResult}"
        }
    }
}
